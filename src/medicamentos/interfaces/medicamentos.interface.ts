export interface Medicamento {
    readonly _id: string;    
    readonly und_medida: string;
    nome: string;
    descricao: string;
    estoque: string;
    dataValidade: string;
    preco: string;
}